services:
  - type: web
    name: market-catalyst-monitor
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: CONTACT_EMAIL
        sync: false
